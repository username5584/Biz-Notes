# Use npm as package manager
package-lock=true

# Save exact versions
save-exact=false

# Use node_modules structure
node-linker=hoisted

# Speed up npm install — skip audit and fund checks
audit=false
fund=false

# Prefer cached packages when available
prefer-offline=true
